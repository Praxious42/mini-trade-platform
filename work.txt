DAY 5 — Execution simulator: price generation + fill decisions
Goal: Turn created orders into filled/rejected.
Checklist:
- Deterministic price generator per symbol (seeded RNG)
- MARKET fills immediately; LIMIT fills if price crosses
- Optionally split into 2 partial fills
Acceptance:
- Unit tests for MARKET and LIMIT behavior


---

DAY 6 — Execution-service: publish orders.filled / orders.rejected
Goal: Emit result events.
Checklist:
- Publish OrdersFilled with fillId(s), qty, price, timestamps
- Publish OrdersRejected for simulated failures
- Ensure idempotency strategy defined (eventId/fillId uniqueness)
Acceptance:
- Creating an order leads to a filled event visible on Kafka


---

DAY 6 — Order-service: consume fill/reject events and update status
Goal: Keep order status in sync.
Checklist:
- Kafka consumer for orders.filled and orders.rejected
- Update order status: PARTIALLY_FILLED/FILLED/REJECTED
- Store fill records (optional) in fills table + Flyway migration V2
Acceptance:
- Integration test with embedded Kafka: create order -> status becomes FILLED


---

DAY 7 — Portfolio-service: DB schema + entities
Goal: Own portfolio state.
Checklist:
- Flyway V1: accounts, account_limits, positions, processed_events
- JPA entities + repositories
- Seed data strategy for demo (data.sql or migration)
Acceptance:
- Can create account + limits and read them from DB


---

DAY 7 — Portfolio-service: consume orders.filled and apply to positions
Goal: Update positions transactionally.
Checklist:
- Kafka consumer for orders.filled
- Upsert (accountId, symbol) position and recompute avg price
- Persist processed_events for idempotency
Acceptance:
- Duplicate event does not double-apply (tested)


---

DAY 8 — Risk engine: margin + position limits
Goal: Implement risk checks used pre-trade.
Checklist:
- Compute notional = qty * priceHint
- requiredMargin = notional * marginRate
- Deny if requiredMargin > availableMargin (equity - usedMargin placeholder)
- Deny if abs(newNetQty) > maxPositionPerSymbol
Acceptance:
- Unit tests cover allow/deny scenarios and edge cases


---

DAY 8 — gRPC API: define .proto + generate stubs
Goal: Portfolio exposes CheckOrderRisk over gRPC.
Checklist:
- Add .proto in contracts or portfolio module
- Define CheckOrderRisk request/response (allowed, reason, requiredMargin, availableMargin)
- Generate Java stubs (protobuf-maven-plugin)
Acceptance:
- Portfolio-service starts gRPC server; basic client call works in test


---

DAY 9 — Order-service: gRPC client to portfolio risk check
Goal: Pre-trade validation via gRPC.
Checklist:
- Add gRPC client config (host/port via env)
- On POST /orders call CheckOrderRisk
- If denied: persist REJECTED + return 409/422 with reason
Acceptance:
- Integration test: order rejected when limits exceeded


---

DAY 9 — End-to-end demo flow (compose)
Goal: Smooth demo of create -> fill -> portfolio update.
Checklist:
- Compose wires service-to-service networking
- Script or curl examples in README
Acceptance:
- One command starts stack; curl shows order FILLED and position updated


---

DAY 10 — Testcontainers: integration tests per service
Goal: “Senior” testing story.
Checklist:
- Order-service: Postgres + Kafka Testcontainers
- Portfolio-service: Postgres + Kafka Testcontainers
- Execution-service: Kafka Testcontainer (optional)
Acceptance:
- `mvn verify` runs integration tests locally


---

DAY 10 — BDD-style scenarios (Cucumber or Given/When/Then JUnit)
Goal: Show TDD/BDD mindset.
Checklist:
- Scenario 1: market order accepted and filled
- Scenario 2: order rejected due to margin
- Scenario 3: duplicate fill event idempotent
Acceptance:
- Scenarios run in CI and pass deterministically


---

DAY 11 — Reliability: retries, DLQ, idempotency hardening
Goal: Production-ish behavior for Kafka flows.
Checklist:
- Configure retries/backoff in consumers
- Route poison messages to DLQ topic
- Add processed_events usage where missing
Acceptance:
- Test demonstrates failed message goes to DLQ (can be lightweight)


---

DAY 11 — Security (optional but nice): simple JWT on order REST API
Goal: Demonstrate Spring Security.
Checklist:
- Add JWT filter (resource server) or simple symmetric token validation
- Protect POST /orders and cancel endpoints
Acceptance:
- Unauthorized request returns 401; authorized succeeds


---

DAY 12 — CI/CD: GitHub Actions pipeline
Goal: CI automation box checked.
Checklist:
- Workflow: build, unit tests, integration tests, jacoco coverage
- Cache Maven deps
- (Optional) build Docker images
Acceptance:
- PR/commit triggers pipeline; green build required


---

DAY 12 — Docker images for each service
Goal: Containerize.
Checklist:
- Dockerfile per service (layered jar or spring boot buildpacks)
- Compose uses built images
Acceptance:
- `docker compose up --build` works from clean checkout


---

DAY 13 — Kubernetes manifests (minimal)
Goal: K8s experience checkbox.
Checklist:
- k8s/ directory: deployments + services + configmaps
- Document how to run (kind/minikube) in README
Acceptance:
- Manifests are valid; services start (even if kafka/postgres remain “dev only”)


---

DAY 13 — Metrics + dashboards (lightweight)
Goal: Observability polish.
Checklist:
- Enable Prometheus endpoint via Actuator
- Expose JVM + http server metrics
- (Optional) add a minimal Grafana/Prometheus compose profile
Acceptance:
- /actuator/prometheus returns metrics


---

DAY 14 — Documentation: architecture + tradeoffs + runbooks
Goal: Make it readable to reviewers.
Checklist:
- README: architecture diagram (ASCII ok), service roles, topics, DB schemas
- “Tradeoffs” section: outbox, exactly-once, ordering, idempotency
- Runbook: how to run locally, how to run tests, sample curls
Acceptance:
- A new reader can run demo in <10 minutes


---

DAY 14 — Final hardening + polish sweep
Goal: Ship quality.
Checklist:
- Lint/format pass
- Remove TODOs, add error messages and consistent HTTP status codes
- Verify cancel behavior + edge cases
- Tag release v0.1.0
Acceptance:
- Demo script works end-to-end and tests are green

